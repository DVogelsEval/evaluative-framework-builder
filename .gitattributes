# Normalise line endings so the repo is consistent across Windows/macOS/Linux
# (R-020). Text files are stored with LF in the repo; binaries are left as-is.
* text=auto eol=lf

*.png binary
*.ico binary
*.woff2 binary
