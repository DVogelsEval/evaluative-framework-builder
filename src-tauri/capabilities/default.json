{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core webview permissions for the main window. No filesystem, shell, or network plugins — the app is self-contained (Arch §7, C-7).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
